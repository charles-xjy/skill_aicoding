# 搜索报告来源编号与正文引用处理说明

本文记录一次“来源列表每行展示正常，但编号重复、正文引用对不上”的问题，供本项目和 UrbanAgent 复用。

## 问题现象

最终报告的来源列表可能出现：

```text
[1] 来源 A
[2] 来源 B
[7] 来源 C
[8] 来源 D
[7] 来源 E
[8] 来源 F
```

即使每个链接已经独占一行，编号仍然重复。更隐蔽的问题是：正文里的 `[7]`、`[8]` 也可能没有对应到最终展示的那一条来源。

## 根因

搜索智能体通常会执行多轮搜索。每轮结果可能从 `[1]` 开始编号，最后再由搜索报告拼接多个来源区段。因此，单个搜索报告内部就可能有重复编号。

Supervisor 合并多个 `search_agent` 结果时，如果使用下面这种方式计算偏移量：

```python
offset += max(re.findall(r"\[(\d+)\]", report))
```

就会产生两个问题：

1. `max()` 不是来源数量。报告可能只有 3 个唯一 URL，但最大引用编号是 10。
2. 原始编号重复时，简单地给所有 `[n]` 加偏移并不能判断它们实际指向哪个 URL。

只在前端把来源文本拆成多行，解决的是展示问题，不会解决编号语义问题。

## 正确处理原则

合并搜索报告时，必须同时处理“来源列表”和“正文引用”，不能只修改其中一个：

1. 找到 `## 来源` 区段。
2. 从每行解析原始编号、标题和 URL。
3. 以 URL 去重；同一 URL 只保留一条。
4. 为唯一来源分配新的连续编号。
5. 建立 `原始编号 -> 新编号` 映射。
6. 用同一映射替换报告正文中的 `[n]`。
7. 再将下一份搜索报告的 offset 增加“唯一来源数量”，而不是最大编号。

本项目的实现位于：

```text
xiongan_agent/supervisor_agent/supervisor_agent_main.py
```

核心函数：

```python
_renumber_search_report(text, offset)
```

它返回：

```text
(重编号后的报告, 本报告唯一来源数量)
```

## 推荐输入格式

搜索报告最好直接输出：

```markdown
## 来源

- [1] 官方公告标题 - https://example.com/a

- [2] 政策解读标题 - https://example.com/b
```

约束要点：

- 每条来源独占一行；
- 使用 Markdown 列表；
- 编号只在当前搜索报告内部使用；
- URL 必须完整保留；
- 正文引用必须使用同一编号；
- 不要在一个报告中重复输出同一 URL。

## 合并后的目标格式

Supervisor 合并完成后，最终报告应当类似：

```markdown
雄安新区建设投资持续增长[1]，重点片区公共服务逐步完善[3]。

## 来源

- [1] 官方建设进展 - https://www.xiongan.gov.cn/example-a

- [2] 容东片区建设报道 - https://example.com/example-b

- [3] 公共服务进展 - https://example.com/example-c
```

正文中的 `[1]`、`[3]` 必须分别指向最终来源列表中的第 1、3 条，不能只保证数字看起来连续。

## 前端展示层的职责

前端可以做展示兜底，但不应该负责决定引用语义：

- 将压成一段的来源拆成逐条列表；
- 对长 URL 使用 `overflow-wrap: anywhere`；
- 允许历史消息在渲染时改善格式；
- 不要在前端重新给来源编号，否则容易与正文引用再次不一致。

本项目的前端兜底位于：

```text
xiongan_frontend/ui/src/components/thread/markdown-text.tsx
xiongan_frontend/ui/src/components/thread/markdown-styles.css
```

## UrbanAgent 对接建议

将相同逻辑放在 UrbanAgent 的“最终报告合并节点”，而不是只放在 UI：

1. 搜索子任务完成后先标准化来源区段；
2. Supervisor 汇总时按 URL 去重和连续编号；
3. 同步替换正文 `[n]` 引用；
4. 最后再把统一来源列表交给分析模型；
5. 分析模型输出后，如果缺少来源列表，只能补充已建立的统一列表，不要重新编号；
6. 增加自动测试，至少覆盖：
   - 两份报告都从 `[1]` 开始；
   - 同一报告中重复 URL；
   - 多轮搜索产生重复原始编号；
   - 正文引用和来源列表同时重编号；
   - Markdown 列表来源与普通文本来源混用。

## 验证清单

完成修改后，使用一个包含多个搜索任务的请求验证：

- 来源编号从 `[1]` 开始连续递增；
- 来源列表没有重复编号；
- 同一搜索报告中的重复 URL 只出现一次；
- 正文中的每个 `[n]` 都能在来源列表中找到；
- 每条来源独占一行；
- 窄屏和局域网浏览器中长 URL 不会撑破消息气泡。
